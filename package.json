{
  "name": "spike",
  "version": "2.0.0-dev",
  "private": true,
  "type": "module",
  "bin": {
    "spike": "./bin/spike"
  },
  "scripts": {
    "spike": "bun src/cli.ts",
    "test": "bun test src test/scenario/direct-cli.test.ts --parallel=8",
    "test:integration": "bun test test/scenario test/contract --parallel=8",
    "test:full": "bun test --path-ignore-patterns test/docker --parallel=8",
    "typecheck": "tsc --noEmit",
    "build:check": "bun build src/cli.ts --target=bun --outfile=/tmp/spike-check.js && bun build src/pi-supervisor-extension.ts --target=node --outfile=/tmp/spike-supervisor-extension-check.js && bun build src/pi-goal-planner-extension.ts --target=node --outfile=/tmp/spike-goal-planner-extension-check.js && bun build src/pi-worker-extension.ts --target=node --outfile=/tmp/spike-worker-extension-check.js",
    "check": "bun run typecheck && bun run test && bun run build:check",
    "check:full": "bun run typecheck && bun run test:full && bun run build:check"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
