{
  "name": "spike",
  "version": "2.0.0-dev",
  "private": true,
  "type": "module",
  "bin": {
    "spike": "./bin/spike"
  },
  "scripts": {
    "spike": "bun src/cli.ts",
    "test": "bun test --path-ignore-patterns test/docker --parallel=8",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun run test && bun build src/cli.ts --target=bun --outfile=/tmp/spike-check.js && bun build src/pi-supervisor-extension.ts --target=node --outfile=/tmp/spike-supervisor-extension-check.js"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
